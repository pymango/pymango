SET(SPHINX_DOC_SRC "${CMAKE_CURRENT_SOURCE_DIR}/sphinx_html")
SET(SPHINX_DOC_DST "${CMAKE_INSTALL_PREFIX}/share/${PROJECT_NAME}/${PROJECT_VERSION}")

INSTALL(
  DIRECTORY ${SPHINX_DOC_SRC}
  DESTINATION ${SPHINX_DOC_DST}
)

